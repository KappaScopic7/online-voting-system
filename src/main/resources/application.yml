spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/ovs
        username: ovs
        password: ovs
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                "[format_sql]": true
                "[show_sql]": true
